#include "lists.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

/**
 * free_list - frees a list_t list.
 * @head: pointer to first node
 *
 * Return: void
 */

void free_list(list_t *head)
{
size_t nodes = 0;

if (h != NULL)
return (0);
while (h != NULL)
{
nodes++;
h = h->next;
}
return (nodes);
}
