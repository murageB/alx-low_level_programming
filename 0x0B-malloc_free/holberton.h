#ifndef HOLBERTON_H
#define HOLBERTON_H

/**
 *_putchar-putchar
 */
int _putchar(char c);

/**
 *create_array-creates an array of char, and initializes it with a specific char.
 */
char *create_array(unsigned int size, char c);

/**
 *_strdup-returns an allocatted space in mem, which contains a duplicate string
 */
char *_strdup(char *str);



#endif
